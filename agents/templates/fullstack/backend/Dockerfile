# Local dev: `python -m uvicorn main:app --reload --port 8000`
# Production: handled by the Ojas deploy pipeline (systemd unit, gunicorn
# would be a swap-in here for heavier apps but uvicorn is fine for v1).

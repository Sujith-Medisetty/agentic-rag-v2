# LangChain / LangGraph core
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-anthropic>=0.3.0
langchain-community>=0.3.0

# MCP integration
langchain-mcp-adapters>=0.1.0
mcp>=1.0.0

# LangGraph persistence (cross-run checkpointing)
langgraph-checkpoint-sqlite>=1.0.0

# Observability
langsmith>=0.1.0

# Existing deps
requests>=2.31.0

# Utilities
pydantic>=2.0.0
python-dotenv>=1.0.0

# Web backend (Phase 1 — replaces CLI as primary user surface)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
websockets>=12.0
itsdangerous>=2.1.0   # signed device-pair tokens
